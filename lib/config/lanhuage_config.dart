import 'package:flutter/material.dart';

class LanguageConfig {

  static List<Locale> android = [
    const Locale('zh', 'CH'),
    const Locale('en', 'US'),
  ];
  static List<Locale> ios = [
    const Locale('en', 'US'),
    const Locale('zh', 'CH'),
  ];
}
